doctype html
html(lang='en')
    head
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no')
        title= title
        link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css')
        style.
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }
            body {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                min-height: 100vh;
                display: flex;
                justify-content: center;
                align-items: center;
                padding: 20px;
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            }
            .error-container {
                background: white;
                padding: 40px 25px;
                border-radius: 20px;
                text-align: center;
                max-width: 400px;
                width: 100%;
                box-shadow: 0 10px 40px rgba(0,0,0,0.3);
            }
            .error-icon {
                font-size: 80px;
                color: #dc3545;
                margin-bottom: 20px;
                animation: shake 0.5s ease-in-out;
            }
            @keyframes shake {
                0%, 100% { transform: translateX(0); }
                25% { transform: translateX(-10px); }
                75% { transform: translateX(10px); }
            }
            .error-title {
                font-size: 24px;
                font-weight: 700;
                color: #2c3e50;
                margin-bottom: 15px;
            }
            .error-message {
                font-size: 16px;
                color: #495057;
                line-height: 1.6;
                margin-bottom: 10px;
            }
            .error-hint {
                font-size: 14px;
                color: #6c757d;
                line-height: 1.5;
            }
    
    body
        .error-container
            i.fas.fa-exclamation-triangle.error-icon
            .error-title Campaign Not Found
            .error-message= message
            .error-hint This campaign may have ended or the QR code link is incorrect.